{
  "name": "seek-challenge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "babel src -d build",
    "start": "yarn test",
    "test": "yarn prettier && jest --coverage",
    "prettier": "prettier --write 'src/**/*.js'"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.0",
    "@babel/core": "^7.2.0",
    "@babel/plugin-proposal-class-properties": "^7.2.1",
    "@babel/preset-env": "^7.2.0",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "jest": "^23.6.0",
    "prettier": "^1.15.3",
    "sinon": "^7.2.0"
  },
  "dependencies": {
    "@babel/polyfill": "^7.0.0"
  }
}
